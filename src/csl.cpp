#include <iostream>
#include <fstream>
#include <string>

int main(int argc, char* argv[]){
    if (argc == 1) {
        
        while (true) {
            std::string cmd;
            printf("CSL>>: ");
            std::cin >> cmd;
        }
    } else if (argc == 2)
    {
        
    }
    return 0;
}