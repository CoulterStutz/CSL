#include <iostream>
#include <fstream>
#include <string>

int main(int argc, char* argv[]){
    if (argc == 1) {
        
        while (true) {
            std::string cmd;
            printf("CSL>>: ");
            std::cin >> cmd;
        }
    }
    return 0;
}